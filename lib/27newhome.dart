import 'package:flutter/material.dart';

class newhome extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Text("Hi welcome to home page"),
      ),
    );
  }

}