import 'package:flutter/material.dart';

class bottomAnime extends StatefulWidget{
  @override
  State<bottomAnime> createState() => _bottomAnimeState();
}

class _bottomAnimeState extends State<bottomAnime> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(), //destination screen

    );
  }
}